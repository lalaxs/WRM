# 文档—代码对齐核查（2026-07-27）

> 核查范围：项目内"规范/状态/设计"类文档（SKILL、MEMORY、README、BASELINE、权威设计文档、框架审查报告）对**当前游戏代码实际状态**的描述是否准确、彼此是否一致。
> 代码基线时间：2026-07-27 晚（已落实离线结算修复、H1 删 Canvas 死代码、H2 突破合规、M1 广告折中桥接、M2 双调色板收口、L3 删旧备份）。

## 一、结论摘要

- **架构/路线类文档（SKILL、README、权威设计文档）与代码基本对齐**；唯一"路线冲突"（广告）已由你在 M1 拍板「折中：先搭桥不接奖励」收口。
- **项目长期备忘 `MEMORY.md` 严重过时**：其"当前代码状态/美术当前状态"停留在 7/23，未反映 7/27 的全部修复，且有两处**事实性错误**（"原型仍是深色"、"待删 Canvas UI 旧代码"）。最需更新。
- **`BASELINE` / `README` 准确**，无需改；我刚写的框架审查报告部分过时，建议补状态标签。

## 二、代码当前真实状态（对齐基线）

1. UI 100% DOM（25 个 Canvas UI 死代码函数已删，H1）。
2. `#game` 全屏 canvas **仅画背景渐变**；角色立绘经 `drawCharacter` 合成到 **DOM 小 canvas**（顶栏 `avatar` / 创建页 `previewCanvas`），由 `ui.js` 调 `GameAPI.render.drawCharacter`。
3. 调色板单一真相在 `styles.css` 的 `:root`（game.js 的 `THEME` 已删，背景渐变读 CSS 变量，M2）。
4. 离线结算 bug 已修（结算水位线每次在线保存推进到当前时间）。
5. 突破走 `core/breakthrough.js` 权威路径（概率仅三来源、门槛强校验，H2 确认合规）。
6. 广告：`AdManager.js` 已建 + `platform.js` 桥接已接，但**游戏内无任何调用入口、不发奖**（M1 折中）。
7. 登录：`platform.js` 有 `login` 桥接，但 `game.js` / `ui.js` **均未调用**（未接入）。
8. FPS 字已随兜底分支删除而移除；`render` 现只调 `window.UI.renderGame`。
9. 发布铁律：未主动上传（符合）。

## 三、逐文档核查

### 3.1 `MEMORY.md`（最需更新）

| 位置 | 文档描述 | 代码实际 | 严重度 | 建议修正 |
|---|---|---|---|---|
| 第26行 美术"当前状态" | "原型仍是深色（旧 `#15131f` 底 / `#3C3489` 导航），需一次性重换肤" | UI 早已全 DOM + `styles.css` 浅紫调色板，换肤完成 | 🔴 高（事实错误） | 改为"换肤已完成，UI 走 `styles.css` 浅紫调色板" |
| 第25 / 48-49 行 渲染架构 | "游戏世界（角色/场景/动画）仍 Canvas2D 手绘" | 场景/动画是 DOM 卡片；Canvas 仅背景渐变 + 立绘 blit 到 DOM 小 canvas | 🟠 中（夸大 Canvas 用途） | 改为"Canvas 仅画全屏背景渐变；角色立绘经 `drawCharacter` 合成到 DOM 小 canvas；游戏世界表现由 DOM 卡片承载" |
| 第51行 调色板 | "从 game.js 的 THEME 常量迁移到 styles.css"（进行时态） | THEME 已删除，背景读 `:root` 变量（M2 完成） | 🟠 中 | 改为"已迁移完成，game.js 不再含 THEME" |
| 第52行 广告 | "广告 API 封装进 AdManager.js" | AdManager 已建但**入口未接、不发奖**（折中） | 🟠 中 | 补"当前：桥接已建、入口未接（M1 折中）；接奖励时调 `AdManager.showReward()`" |
| 第55行 当前代码状态 | 整段停留在 7/23：待办列"删 Canvas UI 旧代码 / THEME / AdManager / 登录 / 清杂质"，且说"#game 画背景渐变+兜底 FPS 字" | 删 Canvas UI✅ / THEME✅ / AdManager 桥接✅(入口未接) / 登录❌未接 / 清杂质⚠️部分；FPS 字已移除 | 🔴 高（严重过时） | 整体刷新到 7/27：已完成项打勾 + 剩余待办（登录未接、NIE 遗留）+ 移除 FPS 字 + 补离线修复/H1/H2/M1/M2/L3 记录 |

### 3.2 `SKILL.md`（与代码对齐，路线表述需补折中说明）

- 架构铁律（混合渲染、DOM UI、`:root` 调色板、`tap.*` 仅在 `platform.js`、`AdManager` 封装）——**与代码一致** ✓。
- 广告规定"走开发者中心内置变现、AdManager.js 封装"——与代码"建桥接未接入口"部分一致；但与**权威设计文档（Stage 6 明确不实现 ad reward）冲突**。该冲突已由你拍板「折中：先搭桥不接奖励」收口，文档保持现状。
- **建议**：在 SKILL 广告节补一句"若权威设计文档（docs/superpowers）某阶段明确不实现广告奖励，以该阶段计划为准；本 SKILL 的广告规定为通用能力预留"，避免后续 Agent 误读为"必须接广告奖励"。

### 3.3 `BASELINE_IMPLEMENTATION_STATUS.md`（准确，无需改）

- 描述的是设计目标 / 开发边界 / 已实现系统清单，与权威 spec/stage 一致；声称的"已完成系统"范围与代码框架对齐（框架审查已确认）。
- 小提示：第132行"release/NIE 保持原样"——`NIE/` 是 Godot 遗留 `.png.import`（L2 待清理），与"保持原样"不矛盾，清理 L2 时再同步说明即可。

### 3.4 `README.md`（准确）

- 准确列出权威文档清单，声明旧文档已退出分支。与代码无描述冲突。✓

### 3.5 权威设计文档 `docs/superpowers`（已对齐，见框架审查报告）

- 渲染架构"游戏世界用数据/卡片、Canvas 仅角色立绘" ↔ 代码（背景渐变 + 立绘 blit + DOM 卡片）✓。
- 广告"Stage 6 不实现 ad reward" ↔ 代码"折中建桥接未接奖励"✓（不偏离）。
- 突破"概率仅三来源 + 门槛校验" ↔ `core/breakthrough.js` 合规 ✓。

### 3.6 框架审查报告 `docs/框架审查报告_2026-07-27.md`（部分过时）

- 报告写于处理 H1/H2/M1 之前，第四节"问题列表"把 M2/L3 标为"待处理"，但**后续已处理**；第二节"符合项"说"UI 全 DOM"是架构意图，当时代码还有 25 个 Canvas `draw*` 函数（H1 尚未做），易误导。
- 报告第六、七节已追加 H1/H2/M1/M2/L3 处理记录，但第四节状态标签未同步。
- **建议**：加"八、截至 7/27 晚状态总览"或把各问题标 `[已处理]`。

## 四、对齐偏差汇总（按严重度）

- 🔴 高：`MEMORY` 第26行"原型仍是深色"事实错误；第55行"当前代码状态"严重过时（未反映 7/27 全部修复）。
- 🟠 中：`MEMORY` 第25/48 行渲染描述夸大 Canvas 用途；第51行调色板"迁移中"应改"已完成"；第52行广告缺"入口未接"状态。
- 🟡 低：`SKILL` 广告节缺"以权威文档阶段计划为准"的折中说明；审查报告第四节状态标签未同步。

## 五、建议修正动作（待确认）

1. **可直接更新（事实性）**：`MEMORY.md` 第26行（换肤已完成）、第51行（THEME 已删）、第55行（整体刷新到 7/27 状态）。
2. **建议确认后改（涉及路线表述）**：`MEMORY` 第25/48 渲染描述、第52行广告状态；`SKILL` 广告节补折中说明。
3. **可选**：审查报告补状态总览。

> 注：以上"建议修正"均不改变游戏代码，只修正文档描述，使其与 7/27 代码真实状态一致。
